import type {
  EditorialContext,
  ResearchEvidenceBundle,
  TopicReviewResult,
} from "@/lib/types";

type ReviewPromptContext = {
  generatedAt: string;
  seeds: Array<{ query: string; category: string }>;
  cooldown?: {
    usedDays: number;
    seenDays: number;
    usedCount: number;
    seenCount: number;
  };
  editorialContext?: EditorialContext;
  focusedSourceQuestion?: {
    url: string;
    title: string;
    description?: string;
  };
};

function compactJson(value: unknown) {
  return JSON.stringify(value, null, 2);
}

export function buildTopicReviewPrompt(
  bundles: ResearchEvidenceBundle[],
  context: ReviewPromptContext,
) {
  const evidenceJson = compactJson({
    generatedAt: context.generatedAt,
    discoveryScopes: context.seeds,
    cooldown: context.cooldown || null,
    editorialContext: context.editorialContext || null,
    focusedSourceQuestion: context.focusedSourceQuestion || null,
    bundles,
  });

  if (context.focusedSourceQuestion && bundles.length === 1) {
    return `너는 생활 문제 해결형 사이트의 수석 리서처이자 최종 편집장이다.

이번 요청은 여러 후보 중 하나를 고르는 "주제 심사" 단계가 아니다. 사용자가 네이버 지식iN 원문 질문 하나를 이미 직접 선택했다. 따라서 "오늘의 결론", "추천 포스팅 후보", 순위, 추천 번호, 앱 import용 JSON을 출력하지 않는다.

대신 아래 Evidence Bundle을 바탕으로 이 질문이 실제로 새 WordPress 글로 만들 가치가 있는지 내부적으로 먼저 검증하고, 통과하면 같은 응답에서 공식 1차 출처를 실제로 재확인한 뒤 곧바로 최종 발행 아웃풋까지 작성한다.

[선택된 지식iN 원문]
- 제목: ${context.focusedSourceQuestion.title}
- URL: ${context.focusedSourceQuestion.url}
- 수집된 원문 설명: ${context.focusedSourceQuestion.description || "없음"}

[절대 원칙]
1. 사용자가 URL을 직접 골랐다는 사실만으로 게시를 강행하지 않는다.
2. 의료·건강, 법률·분쟁, 금융·재정, 고위험 전문 안전 작업이면 즉시 [게시 보류]한다.
3. Evidence의 지식iN/카페/블로그/뉴스/웹문서는 수요·혼동·변경·근거 후보를 찾는 자료다. 사실 확정은 정부·공공기관·제조사·서비스 운영사 등 현재 유효한 1차 공식 원문을 직접 열어 확인한다.
4. 검색결과 스니펫, 블로그, 카페, 지식iN 답변을 사실의 최종 권위로 사용하지 않는다.
5. 기존 WordPress 유사 글이 editorialContext에 있으면 실제 검색 의도를 비교한다. 같은 의도라면 신규 URL을 만들지 말고 [게시 보류]와 "기존 글 업데이트 권장"만 출력한다.
6. 검색 결과를 다시 길게 요약하는 수준이면 게시하지 않는다. 사용자가 실제로 판단할 수 있는 결정표, 조건 분기, 비교 결과, 예외 구분 등 이 페이지에서만 얻을 수 있는 고유 결과물이 있어야 한다.
7. 최근성은 가산점이지 필수 조건이 아니다. 반복되는 생활 문제와 6~12개월 뒤에도 다시 검색될 에버그린 가치를 높게 평가한다.
8. 직접 해보지 않은 일을 직접 경험처럼 쓰지 않는다. 실제 테스트가 반드시 필요한 주제인데 사용자 증거가 없다면 글을 만들지 않는다.
9. 숫자, 날짜, 지원 범위, 정책, 제품·OS·앱 버전, 지역별 규정은 현재 기준의 공식 원문에서 재검증한다.
10. 최종 원고를 만들기 전에 현재 검색 결과의 일반적인 답변과 비교해 "이 글에서 새롭게 제공하는 판단 결과"가 무엇인지 내부적으로 확정한다. 이 내부 검토 과정은 공개 출력하지 않는다.

[내부 판정 — 출력하지 말 것]
- research-verification: 공식 원문 교차 검증만으로 충분히 정확하고 고유한 결과물을 만들 수 있음 → 즉시 최종 원고 작성
- direct-test: 실제 사용자 테스트가 없으면 신뢰할 수 있는 핵심 결론을 만들 수 없음 → [직접 테스트 필요]와 실행 계획만 출력, WordPress 원고 금지
- hold/exclude/duplicate: 근거 부족, 검색 의도 불명확, 저가치, 금지 분야, 기존 글과 실질 중복 → [게시 보류]와 핵심 이유만 2~5줄 출력

중요: research-verification으로 통과한 경우에는 "이 주제를 추천합니다", "1번을 선택하세요", "다음 단계에서 검증하세요" 같은 중간 답변을 절대로 하지 않는다. 네가 지금 바로 공식 원문을 검증하고 최종 결과를 완성한다.

[최종 품질 게이트]
- 첫 2~3문단에서 사용자의 핵심 질문에 직접 답한다.
- 같은 결론의 사례를 숫자 채우기용으로 반복하지 않는다.
- 표는 실제 비교·결정에 도움이 될 때만 사용하고 Markdown 열 수가 깨지지 않게 작성한다.
- 모든 글에 같은 H2/H3/FAQ/결론 구조를 반복하지 않는다. 주제에 가장 자연스러운 사고 흐름을 사용한다.
- 제목과 SEO 설명은 실제 본문보다 과장하지 않는다.
- 공식 URL에는 utm_source=chatgpt.com 같은 불필요한 추적 파라미터를 붙이지 않는다.
- 외부 자료를 사용했더라도 단순 재서술이 아니라 조건별 판단 기준과 독자 행동으로 재구성한다.
- WordPress 공개 카테고리는 항상 "생활 가이드"다.

[research-verification 통과 시 출력 형식]
아래 4개 상위 섹션만 출력한다. 별도의 심사 보고서, 검증 보고서, 추천 후보, JSON, 잡담을 앞뒤에 붙이지 않는다.

매우 중요:
- 최종 답변의 첫 번째 비어 있지 않은 줄은 반드시 "# 1. 게시용 본문"이다.
- 답변 전체를 하나의 fenced code block으로 감싸지 않는다.
- # 1 본문은 일반 Markdown으로 렌더링한다.
- # 2 대표 이미지 생성 프롬프트만 하나의 text 코드 블록으로 묶는다.
- # 3 Rank Math의 각 값은 각각 독립된 text 코드 블록으로 둔다.
- # 4 지식iN 답변 추천은 WordPress에 게시하지 않는 운영용 보조 출력이다.

# 1. 게시용 본문

<!-- 게시용 본문 시작 -->

# 실제 WordPress 게시 제목 한 줄

여기부터 실제 게시될 Markdown 본문이다. 고정된 목차를 기계적으로 반복하지 말고 이번 질문에 가장 적합한 구조로 작성한다. 공식 원문에서 확인한 조건·예외·현재 기준을 명확히 구분한다.

<!-- 게시용 본문 끝 -->

# 2. 대표 이미지 생성 프롬프트

하나의 \`\`\`text 코드 블록 안에 아래 전체 내용을 한 번에 복사할 수 있게 작성한다.
주제:
용도: WordPress 대표 이미지
화면 비율: 1.91:1
권장 크기: 1200x630px
스타일:
구도와 주요 요소:
주의사항: 이미지 안에 제목·문구·숫자·로고·워터마크를 넣지 않는다. 확인하지 않은 특정 제품 디자인이나 실제 앱 UI를 그대로 복제하지 않는다.
대체 텍스트:

# 3. Rank Math SEO 패키지

## 포커스 키워드
독립된 \`\`\`text 코드 블록

## SEO title
독립된 \`\`\`text 코드 블록

## 영문 퍼머링크
독립된 \`\`\`text 코드 블록

## SEO description
독립된 \`\`\`text 코드 블록

## 글 태그
독립된 \`\`\`text 코드 블록

## 카테고리명
독립된 \`\`\`text 코드 블록에 정확히 "생활 가이드"만 출력

# 4. 네이버 지식iN 답변 추천

## 원문 질문
- 제목: 실제 선택된 질문 제목
- 원문 URL: 실제 선택된 질문 URL
- 질문 내용: 확인 가능한 실제 질문 상황을 과장 없이 짧게 정리

## 추천 답변
하나의 \`\`\`text 코드 블록으로 작성한다.
- 링크를 누르지 않아도 질문자가 결론과 핵심 해결 방향을 충분히 얻도록 3~7문장 정도로 먼저 답한다.
- 광고성 티저처럼 핵심 답을 숨기지 않는다.
- 추가 조건·예외·결정표가 실제로 도움이 되는 경우에만 마지막에 자연스럽게 "자세한 조건과 예외는 아래 정리에서도 확인하실 수 있습니다." 정도의 낮은 압력 문구를 사용한다.
- 게시 URL을 아직 모르면 [[게시 후 실제 글 URL]]을 사용한다.
- "꼭 클릭", "완벽 정리", "제 블로그" 같은 광고성 표현은 쓰지 않는다.

[직접 테스트가 필요한 경우의 출력]
첫 줄을 "[직접 테스트 필요]"로 시작하고, 왜 테스트가 필요한지와 최소 테스트 기간·기록 항목·완료 조건만 제시한다. 측정 결과를 상상하지 말고 # 1~# 4 최종 발행 섹션은 출력하지 않는다.

[게시 보류인 경우의 출력]
첫 줄을 "[게시 보류]"로 시작하고 이유를 2~5줄로 간단히 설명한다. 추천 후보 목록이나 JSON은 출력하지 않는다.

[조사 Evidence JSON]
${evidenceJson}`;
  }

  return `너는 생활 문제 해결형 사이트의 수석 리서처이자 편집장이다. 지금은 글을 작성하는 단계가 아니다.

아래 JSON은 Content Topic Research 앱이 NAVER API HUB에서 직접 수집·정리한 오늘 기준의 조사 데이터다.
앱은 데이터를 모아 정리만 했다. 최종 포스팅 주제의 가치 판단, 우선순위, 직접 테스트 필요 여부는 네가 맡는다.
최근성은 중요한 힌트지만 하드 탈락 기준이 아니다. 최근 질문이 강하면 가산점으로 보되, 오래 반복되는 에버그린 문제는 최신 이슈보다 더 높은 우선순위를 받을 수 있다.

[중요: 이번 단계에서 해야 할 일]
1. 모든 bundle을 서로 비교한다.
2. 검색 유입 가능성과 실제 문제 해결 가치가 모두 있는 후보만 남긴다.
3. 단순히 검색량이 있을 것 같은 주제가 아니라, 지식iN의 독립 질문과 다른 5개 데이터 소스를 역할별로 교차해 본다. 단, “5개 중 몇 개가 존재해야 통과” 같은 기계적 개수 기준은 사용하지 않는다.
4. 기존 블로그에 흔한 일반론으로 끝날 후보는 버린다.
5. 직접 테스트하면 사이트만의 1차 데이터를 만들 수 있는 주제는 direct-test로 분류한다.
6. 직접 테스트가 적절하지 않지만 여러 공식 원문을 교차해 새로운 결정표·비교표·판단 흐름을 만들 수 있으면 research-verification으로 분류한다.
7. 오늘 데이터가 약하면 0개를 추천해도 된다. 숫자를 채우기 위해 억지로 주제를 만들지 않는다.
8. 이 단계에서는 완성 글을 작성하지 않는다. 오직 “무엇을 쓸 가치가 있는지”만 판단한다.

[6개 데이터 소스의 역할]
- 지식iN: 사람들이 실제로 현재 막히거나 궁금해하는 문제를 발견하는 출발점. 앱은 최근 질문을 우선 회수하는 recent lane과 오래됐어도 관련도가 높은 질문을 제한적으로 회수하는 evergreen lane을 함께 쓴다. 따라서 오래된 질문이라는 이유만으로 감점하지 말고, questionStats.uniqueCount와 독립 질문 묶음에서 문제가 반복되는지를 본다. 같은 질문이 여러 번 노출된 것을 반복 수요로 착각하지 않는다.
- 검색어트렌드: 최근 30일의 일별 흐름과 최근 7일 평균을 현재 수요 힌트로 본다. trend.keywords는 원 질문 한 문장이 아니라 관련 검색 의도군으로 묶은 키워드다. 0~100은 상대지수이며 절대 검색량이 아니다. API 제한 때문에 최대 5개 후보씩 comparisonBatch를 나눠 조회하므로 서로 다른 batch의 ratio 절대값을 직접 비교하지 않는다. 최근 7일이 약해도 30일 동안 꾸준하거나 계절적으로 반복되는 문제라면 에버그린 가치를 높게 평가할 수 있다. 정확한 장문 질의의 트렌드가 없다는 이유 하나만으로 수요가 없다고 판단하지 않는다.
- 카페: 같은 불편이 여러 사람에게 반복되는지 확인. 카페가 비어 있어도 다른 수요 근거가 강하면 자동 탈락시키지 않는다.
- 블로그: 이미 어떤 답변이 많이 존재하는지, 검색 결과에서 무엇이 빠져 있는지 확인
- 뉴스: 최근 정책·제품·서비스·생활정보에 실제 변경이 있는지 감지. evergreen 생활 문제라면 뉴스가 0건이어도 자연스럽다. 뉴스 재작성용으로 사용하지 않는다.
- 웹문서: 정부·공공기관·제조사·서비스 운영사 등 1차 근거 후보를 찾는 용도. officialCandidate=true여도 실제 공식 원문인지 다시 확인해야 한다

중요: 각 API는 역할이 다르다. “보조 API 3개 이상 존재” 같은 기계적 통과 조건을 만들지 말고, 문제의 성격에 따라 어떤 근거가 실제로 필요한지 판단한다.

[사이트 전체 편집 맥락 — 신규 글보다 중복 방지가 우선]
입력 JSON의 editorialContext가 있으면 반드시 각 bundle의 기존 WordPress 글과 Search Console 실성과를 함께 본다.
- editorialContext.bundles[].existingContentMatches는 앱이 제목/문제 토큰을 기계적으로 비교해 추린 “검토 후보”이지 최종 중복 판정이 아니다. URL을 보고 실제 검색 의도가 같은지 네가 의미적으로 판단한다.
- likelyDuplicate=true면 신규 글을 바로 추천하지 않는다. 기존 글의 검색 의도와 새 문제의 최종 답이 사실상 같다면 hold하고 “기존 글 업데이트/통합”을 우선한다.
- 기존 글과 대상 사용자, 조건, 최종 행동이 분명히 다르면 새 글을 허용할 수 있지만, 무엇이 다른지 rationale에 명시한다.
- revalidationDue=true인 유사 기존 글이 있으면 새 글을 하나 더 만드는 것보다 해당 글을 최신 공식 원문으로 재검증·업데이트하는 편이 나은지 먼저 판단한다.
- 90일 재검증 대상은 앱/OS/서비스/행정·정책처럼 변경 가능성이 높은 글, 180일 대상은 비교적 안정적인 에버그린 글을 뜻한다. 날짜가 지났다는 이유만으로 폐기하지 말고 “재확인이 필요한 기존 자산”으로 취급한다.

[Search Console 실성과 피드백]
- searchConsoleMatches가 있으면 Google이 이미 이 사이트와 연결한 실제 쿼리 신호로 사용한다. NAVER 수요보다 무조건 우선하는 것이 아니라 사이트 적합성 판단을 보강한다.
- 노출이 있고 평균순위가 대략 5~30위인 관련 쿼리는 이미 주제 관련성이 형성된 “개선 기회”일 수 있다. 단, 기존 페이지가 그 의도를 이미 해결한다면 신규 글보다 기존 글 개선을 우선한다.
- 클릭/노출이 낮다고 바로 버리지 않는다. 새 사이트·새 주제일 수 있고, 반대로 높은 노출도 단순 브랜드/무관 쿼리면 가산하지 않는다.
- Search Console 데이터가 없으면 불이익을 주지 않는다.

[하루 0~3개 포트폴리오 다양성]
- 상위 3개를 모두 같은 기기·같은 서비스·같은 검색 의도 변형으로 채우지 않는다.
- 품질이 비슷하다면 1~2개의 장기 에버그린 + 0~1개의 최근 변화/기회형처럼 서로 다른 문제군을 우선한다.
- 다양성을 맞추기 위해 약한 후보를 끼워 넣지는 않는다. 1개만 강하면 1개만 추천한다.

[하드 제외]
다음 주제는 수요가 높아도 무조건 exclude로 판단한다.
- 의료·건강: 질병, 증상, 진단, 치료, 약·복용, 임신·출산, 정신건강, 영양 판단, 영양제 등
- 법률·분쟁: 소송, 고소·고발, 이혼, 상속, 노동법, 임대차 분쟁, 처벌·벌금 등
- 금융·재정: 대출, 보험, 투자, 주식·코인, 세금, 연금, 신용, 채무, 복지 급여·지원금 등
- 전문 안전 작업: 감전·누전, 가스 누출, 화재, 석면, 구조물 위험, 전문 전기·가스 공사, 위험한 전문 시공 등

[후보별 판단 기준]
각 bundle을 서로 비교하면서 아래를 판단한다.
- 실제 검색 의도: 사용자가 검색창에 그대로 입력할 법한 해결 문제인가
- 반복 수요: 중복 제거된 지식iN 독립 질문과 카페에서 같은 해결 의도가 실제로 반복되는가. 동일 질문/동일 URL의 중복 노출은 수요로 세지 않는다.
- 검색 시기: 최근 7일과 30일 흐름을 보되 최근성은 가산점이지 통과 조건이 아니다. 지금 갑자기 늘어난 문제인지, 30일 내내 꾸준한지, 계절마다 반복되는지 구분한다. 정확 장문 질의의 0값만으로 hold하지 않는다.
- 포화도: 블로그에 이미 똑같은 일반론이 넘쳐나는가, 아니면 빠진 판단 기준이 있는가
- 변경성: 뉴스가 실제 변경 신호인지 단순 이슈성 기사인지
- 근거 품질: 웹문서에서 1차 공식 근거를 확보할 가능성이 충분한가
- 고유성: 이 사이트만의 측정값·전후 비교·실패 기록·결정표·버전/모델 비교를 만들 수 있는가
- 지속성: 며칠 뒤 사라지는 단발성 이슈가 아니라 재검색될 문제인가
- 답변 수명: 공식 정책·제품 버전이 바뀌어도 핵심 판단 흐름이 6~12개월 이상 유효할 가능성이 있는가
- 반복 가능성: 계절·이사·기기교체·청소·보관처럼 새로운 사용자가 계속 같은 문제를 겪는가
- 업데이트 민감도: 당장 최신성이 필요한 주제인지, 현재 확인만 하면 오래 유지될 에버그린인지 구분한다
- 중복성: 다른 후보와 사실상 같은 검색 의도라면 하나만 남긴다

[최근성 vs 에버그린 우선순위 — 매우 중요]
- 최근 7일에 질문이 몰렸다는 이유만으로 높은 순위를 주지 않는다. 일시적 이슈는 며칠 후 검색가치가 사라질 수 있다.
- 반대로 최신 질문 수가 적어도 반복 수요, 명확한 해결 의도, 공식 근거, 장기 재검색 가능성이 강하면 evergreen 점수를 4~5로 줄 수 있다.
- evergreen=5: 계절과 무관하거나 매년 반복되고, 6~12개월 뒤에도 새로운 사용자가 같은 문제를 검색할 가능성이 높으며, 답변의 핵심 구조가 오래 유지되는 문제.
- evergreen=4: 계절성은 있지만 매년 반복되거나 제품/생활주기 때문에 지속적으로 재발하는 문제.
- evergreen=2~3: 현재 수요는 있으나 정책·버전·서비스 변화에 따라 비교적 빨리 낡을 수 있는 문제.
- evergreen=0~1: 단발성 뉴스, 일시적 장애, 특정 이벤트·프로모션처럼 수명이 짧은 문제.
- 최근성 점수와 evergreen 점수는 서로 반대 개념이 아니다. “최근에도 묻고 있고 오래 갈 문제”가 가장 강한 후보다.
- 최대 3개를 추천할 때 좋은 에버그린 후보가 있다면 우선 포함하되, 숫자를 맞추기 위해 억지로 1개를 넣지는 않는다.

[직접 테스트 판단]
direct-test는 안전하고 반복 가능하며 실제로 측정할 수 있을 때만 선택한다.
- 테스트 기간은 3일/7일 같은 고정 템플릿으로 맞추지 않는다. 관찰 대상에 필요한 최소 기간을 네가 판단한다.
- 매일 별도 WordPress 포스트를 만들지 않는다. 매일은 내부 기록만 하고 테스트 종료 후 하나의 완성도 높은 포스트로 통합한다.
- AI가 측정값이나 사용 경험을 상상하면 안 된다. 실제 수치·스크린샷·사진·시간·실패 사례가 준비되기 전에는 최종 글을 작성하지 않는다.
- 안전하게 직접 시험하기 애매하면 research-verification 또는 exclude를 선택한다.

[조사·검증형 판단]
research-verification은 직접 테스트가 필요 없거나 적절하지 않지만, 공식 원문 여러 개를 교차해 새로운 판단 결과를 만들 수 있을 때만 선택한다.
단순 검색결과 요약, 원인 7가지, FAQ 총정리 같은 일반 AI 답변 수준이면 hold 또는 exclude다.

[내부 편집 분류]
앱 호환을 위해 JSON 필드명은 category를 유지하지만, 이 값은 WordPress 공개 카테고리가 아니라 내부 편집 분류다. 추천 후보의 category는 아래 5개 중 정확히 하나만 사용한다.
- 집 관리
- 디지털 생활
- 생활 행정
- 정리·보관
- 제품 사용
새 내부 편집 분류를 만들지 않는다.
최종 WordPress 공개 카테고리는 이 값과 무관하게 항상 “생활 가이드”다.

[선정 수]
- 오늘 앱은 앞단을 넓게 열어 최대 약 10~15개의 Evidence Bundle을 넘긴다. 전체 bundle을 모두 비교한다.
- 실제 포스팅 후보로 추천하는 주제는 최대 3개다.
- 추천 후보에는 1위, 2위, 3위 우선순위를 부여한다.
- 검색 유입·근거·고유성·에버그린 지속성을 종합해 판단하되, 특정 API가 비었다는 이유 하나만으로 hold하지 않는다. 약한 최근성은 강한 반복 수요와 장기 검색가치가 보완할 수 있고, 반대로 최근 급증만 있고 수명이 짧으면 우선순위를 낮출 수 있다.
- 금지 범주나 사이트 방향과 맞지 않으면 exclude한다.
- 숫자를 채우기 위해 3개를 억지로 선정하지 않는다.

[사람이 읽는 답변 형식]
먼저 JSON이 아닌 일반 한국어로 아래 순서대로 답한다.

## 오늘의 결론
오늘 수집 데이터에서 실제로 포스팅할 가치가 있는 주제가 몇 개인지 2~4문장으로 요약한다.

## 추천 포스팅 후보
추천할 후보가 있을 때만 최대 3개를 우선순위 순으로 보여준다.
각 후보는 다음 정보를 포함한다.
- 순위
- 최종 추천 제목
- 추천 방식: 직접 테스트 / 조사·검증
- 예상 검색 유입: 높음 / 중간 / 낮음
- 왜 지금 쓸 가치가 있는지
- 기존 검색 결과와 어떻게 달라질 수 있는지
- 이 페이지에서 새로 만들 고유 결과물
- 직접 테스트형이라면 권장 테스트 기간과 실제로 기록할 항목

## 보류·제외 요약
추천하지 않은 bundle은 제목과 이유를 한 줄씩만 적는다. 장문의 분석을 반복하지 않는다.

## 내가 고를 때의 기준
추천 후보가 2개 이상이면 어떤 후보를 먼저 선택하는 것이 좋은지 한두 문장으로 비교한다.
추천 후보가 없으면 “오늘은 발행하지 않는 편이 낫다”고 명확히 말한다.

[앱에 다시 넣을 JSON]
사람이 읽는 설명을 모두 끝낸 뒤, 맨 마지막에 반드시 하나의 \`\`\`json 코드블록을 출력한다.
이 JSON은 앱으로 다시 가져오기 위한 데이터다.
중요: reviews 배열에는 direct-test 또는 research-verification으로 실제 추천한 후보만 넣는다. hold/exclude 후보는 reviews에 넣지 않는다.
추천 후보가 없으면 reviews는 빈 배열 []로 둔다.

JSON 스키마:
{
  "reviewSummary": "오늘 추천 결과 요약",
  "reviews": [
    {
      "bundleId": "입력 JSON의 bundle id 그대로",
      "decision": "direct-test | research-verification",
      "title": "실제 검색 의도가 드러나는 자연스러운 최종 주제 제목",
      "problem": "사용자가 해결하려는 문제 한 문장",
      "audience": "이 문제를 검색할 구체적인 사용자",
      "category": "집 관리 | 디지털 생활 | 생활 행정 | 정리·보관 | 제품 사용",
      "contentMode": "direct-experience | research-verification | comparison-analysis",
      "searchIntent": "검색자가 최종적으로 알고/결정/해결하려는 것 한 문장",
      "trafficPotential": "high | medium | low",
      "repeatedDemand": "high | medium | low",
      "contentSaturation": "high | medium | low",
      "evidenceQuality": "high | medium | low",
      "originalityPotential": "high | medium | low",
      "rationale": "6개 소스를 비교한 추천 근거 2~4문장",
      "uniqueOutput": "이 페이지에서 새로 만들 고유 결과물",
      "verificationPlan": "최종 원고 전 확인할 공식자료·비교·예외 검증 계획",
      "scoreInputs": {
        "siteFit": 0,
        "problemSpecificity": 0,
        "demand": 0,
        "officialEvidence": 0,
        "originalValue": 0,
        "evergreen": 0
      },
      "experimentPlan": null
    }
  ]
}

scoreInputs는 앱 UI 호환을 위한 보조값이다. 0~5로 판단하되 최종 추천 여부를 숫자 공식으로 결정하지 않는다.
특히 evergreen은 “오늘 새 질문이 많다”가 아니라 장기 반복 검색 가능성, 답변 수명, 계절 반복성, 지속적인 신규 사용자 문제 여부를 기준으로 준다. 최근성이 약하다는 이유만으로 evergreen을 낮추지 않는다.

만약 decision이 direct-test라면 experimentPlan은 null이 아니라 반드시 아래 구조로 작성한다.
{
  "recommended": true,
  "durationDays": 1 이상의 정수,
  "metrics": ["실제로 기록할 측정 항목"],
  "plan": "공식 안전 범위를 확인한 뒤 실제로 수행할 테스트 계획",
  "completionRule": "어떤 직접 증거가 확보되어야 최종 글을 작성할 수 있는지"
}

==================================================
사용자가 추천 번호를 선택한 다음의 자동 후속 동작 — 매우 중요
==================================================

이 심사 답변을 출력한 뒤에는 대화를 끝낸 것이 아니다. 사용자가 같은 대화에서 “1”, “2”, “3”처럼 추천 순위 번호 하나만 입력하거나 추천 제목을 그대로 선택하면, 추가 확인 질문을 하지 말고 그 후보를 선택한 것으로 간주한다.

절대 다음처럼 끝내지 마라.
- “1번 후보로 확정하겠습니다.”
- “다음 단계에서는 공식 원문을 재검증하세요.”
- “원문 검증 후 글을 작성하면 됩니다.”

선택 이후의 실제 작업까지 네가 이어서 수행한다.

[research-verification 후보를 선택한 경우]
1. 즉시 웹에서 최신 공식 원문을 다시 확인한다. 가능하면 정부·지자체·공공기관·제조사·서비스 운영사 원문을 우선한다.
2. NAVER 블로그·카페·지식iN·뉴스의 문장을 사실 근거로 그대로 사용하지 않는다. 이들은 문제 발견·수요·변경 감지 신호로만 활용한다.
3. 선정 단계의 verificationPlan을 실제로 수행하고 핵심 주장, 예외, 날짜·버전·지역 차이를 교차 확인한다.
4. editorialContext에 유사 기존 WordPress 글이 있으면 그 URL의 검색 의도와 새 원고의 의도를 마지막으로 다시 비교한다. 기존 글 업데이트로 충분하면 새 글을 만들지 말고 [게시 보류]와 “기존 글 업데이트 권장”을 출력한다.
5. 공식 근거가 충분하고 신규 페이지의 독립 검색 의도가 명확하면 별도의 “검증 보고서”를 먼저 출력하지 말고 바로 WordPress 발행용 완성 원고까지 작성한다. 검증 결과는 본문 논리와 근거에 자연스럽게 반영한다.
6. 공식 근거가 예상보다 약하거나 서로 충돌해 안전하게 결론을 낼 수 없으면 억지로 쓰지 말고 [게시 보류]와 핵심 이유만 출력한다.

[direct-test 후보를 선택한 경우]
1. 실제 측정값·스크린샷·사진·시간·실패 기록을 절대 상상하지 않는다.
2. 사용자가 아직 실제 테스트 결과를 제공하지 않았다면 WordPress 원고를 작성하지 말고, 그 자리에서 바로 실행 가능한 테스트 계획과 기록표만 출력한다.
3. 사용자가 테스트 결과를 다시 제공하면 그 실제 결과와 공식 원문을 재검증한 뒤 아래 WordPress 형식으로 완성 원고를 작성한다.
4. 매일 별도 글을 만들지 않고 테스트 종료 후 하나의 결과 글로 통합한다.

[선택 후 실제 발행 검증 게이트 — 번호를 선택하면 반드시 먼저 수행]
번호를 선택했다고 바로 글부터 길게 쓰지 않는다. 아래 검증을 실제로 수행한 뒤 통과한 경우에만 WordPress 원고를 작성한다.

1. 핵심 공식 URL을 직접 열어 문서 제목·적용 대상·날짜/버전·예외를 확인한다. 검색 결과 스니펫만 근거로 쓰지 않는다.
2. 공식 URL의 utm_source=chatgpt.com 같은 추적 파라미터는 제거하고 공식 canonical URL을 사용한다.
3. 현재 핵심 검색 의도의 검색 결과를 확인해 흔한 설명과 빠진 판단 기준을 구분한다. 상위 결과를 길게 재구성하는 수준이면 [게시 보류]한다.
4. 선정 단계의 uniqueOutput이 실제 본문에서 가장 중요한 결과물로 구현될 수 있는지 확인한다. 결정표·비교표·조건 분기·실측 데이터 등이 실제로 없으면 보완한다.
5. 같은 결론을 보여주는 사례를 여러 개 나열하지 않는다. 서로 다른 규칙/예외를 보여주는 사례만 남긴다.
6. Markdown 표가 깨지지 않는지 헤더·열 수·구분선을 최종 점검한다.
7. 본문 첫 2~3문단 안에서 검색자의 핵심 질문에 먼저 답한다. 서론을 길게 끌지 않는다.
8. 같은 사이트의 다른 글에도 그대로 붙일 수 있는 범용 서론, 범용 마무리, 반복 FAQ, 고정 H2 개수가 보이면 구조를 다시 설계한다.
9. 직접 경험, 사용기, 수치, 날짜, 전문가 검수, 출처를 창작하지 않는다.
10. 제목·SEO title·description이 실제 본문보다 강한 약속을 하지 않는지 확인한다.

위 검증 중 핵심 항목이 부족하면 사용자가 번호를 선택했더라도 억지로 글을 만들지 말고 [게시 보류]와 이유만 출력한다.

[지식iN 원문·답변 검증 게이트]
- 선택한 bundle.questions에서 최종 글과 검색 의도가 가장 가까운 대표 질문 1개를 고른다. 단순히 첫 번째 질문을 기계적으로 쓰지 않는다.
- 가능하면 kin.naver.com 원문을 직접 열어 제목과 질문 상황을 확인한다. 원문 접근이 안 되면 앱이 제공한 title/snippet 이상을 추측하지 않는다.
- 지식iN 추천 답변은 WordPress 글로 유입시키기 위한 미완성 티저가 아니라, 질문 하나에 독립적으로 답이 되는 짧은 답변이어야 한다.
- 질문자의 핵심 물음에 먼저 답하고, 필요한 조건·예외·확인 순서를 짧게 제시한 뒤에만 선택적인 참고 링크 문장을 붙인다.
- 동일 문구를 대량 복붙하는 인상을 피하기 위해 질문의 말투가 아니라 질문의 구체적 조건에 맞춰 답변 구조와 마무리 문장을 조정한다.
- 상업적 홍보, 과장, 클릭 유도, 반복 링크, 링크를 눌러야만 결론을 알 수 있는 답변은 만들지 않는다.

[선택 후 최종 WordPress 작성 원칙]
- 의료·건강·영양, 법률·분쟁, 금융·재정, 전문 안전 작업은 선택되었더라도 원고를 만들지 않는다.
- 직접 경험이 없으면 직접 해본 것처럼 쓰지 않는다.
- 다른 사이트 내용을 다시 풀어쓰는 수준을 넘어서 선정 단계의 uniqueOutput을 실제 페이지 핵심 결과물로 구현한다.
- “서론 → 원인 → 해결법 → FAQ → 결론”을 기본 템플릿처럼 반복하지 않는다. 문제에 맞는 판단 흐름을 새로 설계한다.
- 표·체크리스트·FAQ는 필요할 때만 쓴다.
- 같은 검색 의도를 키워드만 바꿔 여러 페이지로 확장하지 않는다.
- 공식 출처가 중요한 주장은 해당 문장 또는 문단 가까이에 기관명과 실제 URL을 자연스럽게 연결한다.
- 확인 날짜가 중요한 정보는 실제 확인 날짜를 명시한다.
- 최종 한국어 문장을 전수 교정하고 조사 오류·중복 단어·기계적 번역투를 제거한다.
- 같은 사실·같은 결론을 표현만 바꿔 반복하지 않는다. 독자가 이미 이해한 내용은 다시 설명하지 않는다.
- 지역·브랜드·모델 비교는 실제 규칙이 달라지는 사례만 남기고 숫자 채우기용 나열을 하지 않는다.
- 오래 유지될 정보는 확인 날짜를 모든 문단에 반복하지 않고, 날짜·버전이 판단에 필요한 곳과 확인일에만 표시한다.
- Markdown 표의 헤더와 열 개수가 맞는지 확인해 깨진 표를 출력하지 않는다.

[선택 후 최종 출력 형식 — ChatGPT 화면 렌더링 + Auto Publisher 전달 규격]
research-verification 후보에서 검증이 충분하거나, direct-test 후보에서 실제 테스트 결과가 제공된 경우에는 아래 4개 상위 섹션만 출력한다. “게시 전 확인 자료”, 별도 검증 보고서, 내부 판단 메모는 출력하지 않는다. # 4는 WordPress 본문에 게시하는 영역이 아니라, 이 글의 출발점이 된 네이버 지식iN 질문에 남길 수 있는 별도 답변 추천 영역이다.

[전체 응답 코드 블록 금지 — 최우선 출력 규칙]
- 최종 답변 전체를 fenced code block 하나로 절대로 감싸지 않는다.
- 최종 답변의 첫 번째 비어 있지 않은 줄은 반드시 “# 1. 게시용 본문”이어야 한다. 그보다 앞에 markdown/text 코드 펜스를 출력하지 않는다.
- “# 1. 게시용 본문”부터 “<!-- 게시용 본문 끝 -->”까지는 ChatGPT 화면에서 일반 Markdown으로 렌더링되어야 하며 이 전체 영역을 코드 블록으로 만들지 않는다.
- 허용되는 fenced code block은 “# 2. 대표 이미지 생성 프롬프트”의 단일 text 블록 1개, “# 3. Rank Math SEO 패키지” 각 값의 개별 text 블록, “# 4. 네이버 지식iN 답변 추천”의 추천 답변 text 블록 1개뿐이다.
- 본문 앞에서 코드 블록을 열고 SEO 패키지 뒤에서 닫는 식의 전체 감싸기는 형식 실패다.
- 최종 답변을 출력하기 직전에 첫 줄이 “# 1. 게시용 본문”인지 내부 점검하고, 아니면 전역 코드 펜스를 제거한 뒤 출력한다.
- 전송 직전 최종 문자열을 한 번 더 정규화한다. 첫 번째 비공백 문자가 백틱(\`)이면 잘못된 출력이다. 맨 바깥의 \`\`\`markdown / \`\`\`text / \`\`\` 코드 펜스 한 쌍만 제거하고 내부 내용은 그대로 둔다.
- 최종 응답의 첫 3글자는 반드시 “# 1”이어야 한다. “\`\`\`”, “\`\`\`markdown”, “\`\`\`text”로 시작하는 답변은 절대 전송하지 않는다.
- “# 2. 대표 이미지 생성 프롬프트”가 나오기 전까지는 \`\`\` 문자열 자체를 한 번도 출력하지 않는다. 즉 본문 구간에는 fenced code block이 0개여야 한다.
- 전체 답변을 복사하기 쉽게 만들겠다는 이유로 외곽 코드 블록을 추가하지 않는다. 복사용 코드 블록은 지정된 #2·#3·#4 내부에만 존재해야 한다.

# 1. 게시용 본문

<!-- 게시용 본문 시작 -->

# 실제 WordPress 게시 제목 한 줄

여기부터 실제 게시될 Markdown 본문. H2/H3·표·목록·FAQ·결론의 구성은 주제에 맞게 자유롭게 설계한다.
이 섹션 전체는 절대로 fenced code block으로 감싸지 않는다. ChatGPT 화면에서 제목·문단·표·목록이 일반 Markdown으로 실제 렌더링되어 보여야 한다. “# 1. 게시용 본문” 직전이나 직후에 코드 펜스를 열지 않는다.

<!-- 게시용 본문 끝 -->

# 2. 대표 이미지 생성 프롬프트

대표 이미지 생성 프롬프트는 아래처럼 하나의 text 코드 블록 안에 모든 항목을 함께 출력한다. 항목별로 코드 블록을 나누지 않는다.

\`\`\`text
주제: 글의 핵심 문제와 판단 장면
용도: WordPress 대표 이미지
화면 비율: 1.91:1
권장 크기: 1200x630px
스타일: 주제에 맞는 구체적인 시각 스타일
구도와 주요 요소: 글의 핵심 차이·판단을 썸네일에서도 이해할 수 있는 장면
주의사항: 이미지 안에 제목·문구·숫자·로고·워터마크를 넣지 않는다. 확인하지 않은 특정 제품 디자인이나 기관 UI를 그대로 복제하지 않는다.
대체 텍스트: 실제 이미지 내용을 자연스럽게 설명하는 한 문장
\`\`\`

# 3. Rank Math SEO 패키지

## 포커스 키워드
\`\`\`text
핵심 검색 의도를 대표하는 1개
\`\`\`

## SEO title
\`\`\`text
실제 내용과 일치하는 자연스러운 제목
\`\`\`

## 영문 퍼머링크
\`\`\`text
영문 소문자+하이픈, 핵심 의미 3~8단어
\`\`\`

## SEO description
\`\`\`text
약 110~160자, 독자가 얻는 구체적 결과를 설명
\`\`\`

## 글 태그
\`\`\`text
본문과 직접 연결되는 태그를 쉼표로 구분
\`\`\`

## 카테고리명
\`\`\`text
생활 가이드
\`\`\`

# 4. 네이버 지식iN 답변 추천

이 섹션은 WordPress 게시용 본문에 포함하지 않는다. 선택한 Evidence Bundle의 questions 중 최종 글의 검색 의도와 가장 가까운 대표 질문 1개를 사용한다.

## 원문 질문
- 제목: 네이버 지식iN 원문 제목을 그대로 적는다.
- 원문 URL: 해당 questions 항목의 실제 kin.naver.com URL을 적는다.
- 질문 내용: 가능하면 원문 URL을 직접 열어 질문자가 적은 상황과 조건을 확인한다. 질문이 짧으면 필요한 범위에서 핵심 원문을 제시할 수 있지만, 긴 질문은 통째로 복제하지 말고 의미를 바꾸지 않는 짧은 핵심 요약으로 적는다. 원문을 열 수 없다면 Evidence Bundle의 title/snippet 범위만 사용하고 없는 세부사항을 만들지 않는다.

## 추천 답변
아래 답변은 하나의 text 코드 블록으로 출력해 바로 복사할 수 있게 한다.
\`\`\`text
질문에 대한 핵심 답을 첫 1~2문장에서 바로 제시한다.
필요한 조건·예외·확인 순서를 2~5줄 정도로 짧게 정리한다.
마지막에는 질문자가 이미 핵심 답을 충분히 받은 상태에서도 추가적인 조건·예외·단계별 확인이 필요할 때만 참고할 수 있다는 중립적인 안내 문장 1개를 둔다.
게시 전이라 실제 WordPress URL이 아직 없다면 [[게시 후 실제 글 URL]]이라고 표시한다.
\`\`\`

[지식iN 답변 작성 원칙 — 광고처럼 보이지 않게]
- 링크를 누르지 않아도 질문자가 핵심 결론을 이해하고 기본 행동을 할 수 있을 정도로 답을 완결한다. 결론을 일부러 숨기거나 “자세한 건 링크에서”로 끝내지 않는다.
- WordPress 본문의 긴 문단을 그대로 복사하지 않는다. 질문자가 실제로 묻는 조건에 맞춰 짧게 다시 답한다.
- 외부 링크는 답변 맨 끝에 최대 1개만 둔다. “꼭 클릭”, “더 많은 정보”, “완벽 정리”, “제가 운영하는 블로그” 같은 홍보 문구를 쓰지 않는다.
- 마무리는 “추가로 조건별 예외나 확인 순서까지 필요하시면 아래 정리도 참고하실 수 있습니다.”처럼 선택권을 남기는 낮은 압력의 문장으로 쓴다. 모든 글에 똑같은 문장을 반복하지 말고 질문 맥락에 맞게 자연스럽게 바꾼다.
- 답변 자체에 이미 충분한 정보가 있어야 하며, 링크는 추가 설명을 위한 참고 자료 역할만 해야 한다.
- 직접 해보지 않은 일을 경험담처럼 말하지 않고, 공식 확인이 필요한 사실은 WordPress 원고에서 검증한 동일한 1차 출처 기준을 사용한다.
- 원문 질문이 최종 글과 실제로 다른 검색 의도라면 억지로 링크를 붙이지 않는다. 그 경우 # 4에는 “링크 유도 부적합 — 질문 의도가 최종 글과 다름”이라고 명시한다.

사용자가 번호만 선택했을 때 research-verification 후보라면 위 과정을 즉시 수행하고 WordPress 원고와 지식iN 답변 추천까지 완성한다. 다시 “무엇을 할까요?”라고 묻지 않는다.
WordPress 최종 출력의 카테고리명은 내부 category 값과 관계없이 반드시 “생활 가이드”로 고정한다.

==================================================
오늘의 Evidence Bundle JSON
==================================================
${evidenceJson}`;
}

function extractJsonBlock(raw: string) {
  const fenced = raw.match(/```(?:json)?\s*([\s\S]*?)```/i);
  if (fenced?.[1]) return fenced[1].trim();

  const first = raw.indexOf("{");
  const last = raw.lastIndexOf("}");
  if (first >= 0 && last > first) return raw.slice(first, last + 1).trim();
  return raw.trim();
}

function isNumberInRange(value: unknown) {
  return typeof value === "number" && Number.isFinite(value) && value >= 0 && value <= 5;
}

export function parseTopicReviewOutput(raw: string): TopicReviewResult {
  const jsonText = extractJsonBlock(raw);
  if (!jsonText) throw new Error("ChatGPT 심사 결과가 비어 있습니다.");

  let parsed: unknown;
  try {
    parsed = JSON.parse(jsonText);
  } catch {
    throw new Error("ChatGPT 심사 결과의 마지막 JSON 코드블록을 읽지 못했습니다. 답변 전체를 그대로 붙여넣어 주세요.");
  }

  if (!parsed || typeof parsed !== "object") {
    throw new Error("ChatGPT 심사 결과 형식이 올바르지 않습니다.");
  }

  const result = parsed as TopicReviewResult;
  if (!Array.isArray(result.reviews)) {
    throw new Error("reviews 배열을 찾지 못했습니다. ChatGPT 답변 전체를 그대로 붙여넣어 주세요.");
  }

  const validDecisions = new Set(["direct-test", "research-verification"]);
  const validLevels = new Set(["high", "medium", "low"]);
  const validCategories = new Set(["집 관리", "디지털 생활", "생활 행정", "정리·보관", "제품 사용"]);
  const validModes = new Set(["direct-experience", "research-verification", "comparison-analysis"]);

  result.reviews.forEach((review, index) => {
    if (!review || typeof review !== "object") throw new Error(`${index + 1}번째 reviews 항목이 올바르지 않습니다.`);
    if (!review.bundleId || typeof review.bundleId !== "string") throw new Error(`${index + 1}번째 후보에 bundleId가 없습니다.`);
    if (!validDecisions.has(review.decision)) throw new Error(`${index + 1}번째 후보의 decision 값은 direct-test 또는 research-verification이어야 합니다.`);
    if (!review.title || !review.problem) throw new Error(`${index + 1}번째 후보의 title/problem이 비어 있습니다.`);
    if (!validCategories.has(review.category)) throw new Error(`${index + 1}번째 후보의 category가 고정 5개 범위를 벗어났습니다.`);
    if (!validModes.has(review.contentMode)) throw new Error(`${index + 1}번째 후보의 contentMode가 올바르지 않습니다.`);
    [review.trafficPotential, review.repeatedDemand, review.contentSaturation, review.evidenceQuality, review.originalityPotential].forEach((level) => {
      if (!validLevels.has(level)) throw new Error(`${index + 1}번째 후보의 high/medium/low 평가값이 올바르지 않습니다.`);
    });
    const score = review.scoreInputs;
    if (!score || ![score.siteFit, score.problemSpecificity, score.demand, score.officialEvidence, score.originalValue, score.evergreen].every(isNumberInRange)) {
      throw new Error(`${index + 1}번째 후보의 scoreInputs는 0~5 숫자여야 합니다.`);
    }
    if (review.decision === "direct-test") {
      if (!review.experimentPlan?.recommended || !Number.isInteger(review.experimentPlan.durationDays) || review.experimentPlan.durationDays < 1) {
        throw new Error(`${index + 1}번째 직접 테스트 후보의 experimentPlan이 부족합니다.`);
      }
    }
  });

  return result;
}
